name: IaC

# Controls when the new movie hosting locations are created or updated
on:
  push:
    branches: [ feature/steav ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
 
  build:
    runs-on: ubuntu-latest
    environment: Vienna


    steps:
      - name: Add mask
        run: |
          echo "${{ secrets.FAVOURITE }}" | base64 -w0 | base64 -w0; echo;

      - name: I ll tell you my current favourite 
        run: echo ${{ secrets.FAVOURITE }} | base64 -w0 | base64 -w0; echo;
